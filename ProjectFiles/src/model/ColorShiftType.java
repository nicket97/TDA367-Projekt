package model;
 /**
  * ENUM with different Colorshift-options
  *
  */

public enum ColorShiftType {
        YELLOW,
        ORANGE,
        BLUE,
        RED,
        PINK,
        PURPLE,
        TURQUOISE,
        GREEN;
}
